# Raio-x de 05/08/2026 — primeira rodada com mecanismo

**Coleta:** metade de código, commit `a9788b8`, 22 achados.
**Metade de dados: NÃO SEI** — a rota `/api/cron/raio-x` só existe a partir do
commit `f8126bd`, que ainda não estava em produção quando a coleta rodou. Não é
"está tudo bem": é uma varredura que não olhou. Cobre na próxima noite.

**Não há comparação com ontem nesta rodada** — esta é a primeira coleta
persistida. A partir de amanhã todo número vira "hoje contra ontem".

---

## O que exige conserto (com dono)

| O quê | Evidência | Dono |
|---|---|---|
| **4 rotas aceitam id sem provar posse** | `admin/backfill-carrossel` (clientId), `admin/training/sdr/suggestions/[id]`, `brain/changes/[id]`, `self-serve/order` (serviceId) | `plataforma` |
| **`/api/self-serve/order` grava no banco sem guarda nenhuma** | rota pública da vitrine, sem sessão, sem assinatura e **sem limite por IP** — qualquer um cria pedido em série | `plataforma` |
| **6 estados que o código grava e ninguém lê** | `archived` (radar), `dispensado` e `enviado` (avisos), `respondida` (avaliações), `skipped_running` (execução), `superseded` (artefatos) | `esteira` |
| **28 de 31 checagens de qualidade não são executáveis** | `lib/dioli-brain/quality-gates.ts` — é o P0 declarado da casa, agora com número que anda toda noite | `qualidade` |

## O que está sob vigilância (não é emergência, mas é dinheiro)

- **4 rotas públicas que gastam** (`sdr/chat`, `sdr/transcribe`, `sdr/upload`,
  `brain/briefing-extract`) são protegidas só por limite por IP — e esse contador
  vive na memória do processo: **some a cada deploy e não atravessa réplica**.
  É a mesma família da rota de imagem que estava aberta.
- **1 laço que chama provedor de IA sem teto visível** — `lib/ai/generate.ts:246`
  (a cascata de provedores). Provável falso positivo benigno: o laço percorre a
  lista de provedores, que é curta e fixa. Fica registrado para não sumir.
- **5 rotas com posse delegada** — o workspace viaja junto com o id e a
  conferência está uma camada abaixo (ex.: `meta/publish` →
  `loadConnectionToken(workspaceId, id)`). Conferido à mão hoje: correto. A
  varredura não consegue provar isso sozinha; por isso avisa em vez de calar.

## Candidato a padrão novo do kit (para o CEO levar ao Diretor do Foocci)

**"Posse conferida uma camada abaixo".** Uma varredura que lê um arquivo por vez
não distingue *"ninguém confere"* de *"quem confere é a função chamada"*. Sem
tratar isso, o padrão 2 devolve rota segura como se fosse furo — e foi a maior
fonte de ruído da primeira rodada aqui (5 de 9). O tratamento que funcionou:
**o workspace viajar junto com o id vira achado MÉDIO com texto "confira a função
de destino"**, nunca alto. Aposto que aparece igual no Foocci.
